/**
 * CDN Constants - Zero dependencies
 *
 * Shared constants for CDN hosts. Kept separate to avoid
 * dependency chains when only the constants are needed.
 */

export const ESM_HOST = 'https://esm.sh'
export const JSDELIVR_BASE = 'https://cdn.jsdelivr.net/npm/'
export const JSDELIVR_META = 'https://data.jsdelivr.com/v1/package/npm/'
